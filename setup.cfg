# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[metadata]
description-file = README.md

[versioneer]
VCS = git
style = pep440
versionfile_source = calendars/_version.py
versionfile_build = calendars/_version.py
tag_prefix = ""
parentdir_prefix = calendars-